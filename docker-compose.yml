services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - backend/.env
    environment:
      PORT: 8787
      DATABASE_PATH: /app/data/omnicloud.db
      CORS_ORIGIN: ${SERVICE_URL_WEB}
      FRONTEND_URL: ${SERVICE_URL_WEB}
      GOOGLE_REDIRECT_URI: ${SERVICE_URL_WEB}/api/accounts/google/callback
      ONEDRIVE_REDIRECT_URI: ${SERVICE_URL_WEB}/api/accounts/onedrive/callback
      DROPBOX_REDIRECT_URI: ${SERVICE_URL_WEB}/api/accounts/dropbox/callback
      YANDEX_REDIRECT_URI: ${SERVICE_URL_WEB}/api/accounts/yandex/callback
    volumes:
      - omnicloud_api_data:/app/data
    restart: unless-stopped

  web:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        VITE_API_BASE_URL: /api
        VITE_WS_BASE_URL: /ws/uploads
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  omnicloud_api_data:
